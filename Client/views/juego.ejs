<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conecta 4</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 20px;
        }
        .board {
            display: inline-block;
            border: 2px solid #333;
            margin-bottom: 20px;
        }
        .row {
            display: flex;
        }
        .cell {
            width: 50px;
            height: 50px;
            border: 1px solid #ccc;
            background-color: #fff;
            margin: 1px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Conecta 4 - Juego</h1>
    <div class="board">
        <% for (let i = 0; i < 6; i++) { %>
            <div class="row">
                <% for (let j = 0; j < 7; j++) { %>
                    <div class="cell" id="cell-<%= i %>-<%= j %>" onclick="dropToken(<%= j %>)"></div>
                <% } %>
            </div>
        <% } %>
    </div>

    <script>
        // Función para manejar el clic en una celda
        function dropToken(col) {
            console.log('Clic en la columna ' + col);
            // Aquí puedes agregar la lógica para manejar el movimiento del jugador
            // Por ejemplo, enviar el movimiento al servidor y actualizar la interfaz del juego
            // Este es solo un ejemplo básico para la demostración
        }
    </script>
</body>
</html>
 